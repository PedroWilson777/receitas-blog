"use client";
// Slot de anúncio — sem AdSense aprovado ainda, não renderiza nada visível
// (placeholder cinza com "Publicidade" passava impressão pouco profissional).
// Trocar por <ins className="adsbygoogle" .../> quando o AdSense for aprovado.
export default function AdSlot({ size }: { size: "sidebar" | "banner" }) {
  return null;
}
